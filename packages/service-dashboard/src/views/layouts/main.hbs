<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}} - Logosphere Dashboard</title>
    <link rel="stylesheet" href="/css/dashboard.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <script src="https://cdn.socket.io/4.7.5/socket.io.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
</head>
<body>
    <div class="dashboard-container">
        {{#if user}}
            <header class="dashboard-header">
                <div class="header-left">
                    <h1><i class="fas fa-chart-line"></i> Logosphere Dashboard</h1>
                </div>
                <div class="header-right">
                    <span class="user-info">
                        <i class="fas fa-user"></i> {{user.username}} ({{user.role}})
                    </span>
                    <a href="/logout" class="logout-btn">
                        <i class="fas fa-sign-out-alt"></i> Sign Out
                    </a>
                </div>
            </header>
            
            <nav class="dashboard-nav">
                <div class="nav-tabs">
                    <a href="/dashboard" class="nav-tab {{#if (eq activeTab 'logs')}}active{{/if}}">
                        <i class="fas fa-list"></i> Logs
                    </a>
                    <a href="/dashboard/metrics" class="nav-tab {{#if (eq activeTab 'metrics')}}active{{/if}}">
                        <i class="fas fa-chart-bar"></i> Metrics
                    </a>
                    {{#if (eq user.role 'admin')}}
                    <a href="/dashboard/files" class="nav-tab {{#if (eq activeTab 'files')}}active{{/if}}">
                        <i class="fas fa-folder"></i> Files
                    </a>
                    <a href="/dashboard/audit" class="nav-tab {{#if (eq activeTab 'audit')}}active{{/if}}">
                        <i class="fas fa-shield-alt"></i> Audit
                    </a>
                    {{/if}}
                </div>
            </nav>
        {{/if}}
        
        <main class="dashboard-content">
            {{{body}}}
        </main>
    </div>
    
    <script src="/js/dashboard.js"></script>
</body>
</html>