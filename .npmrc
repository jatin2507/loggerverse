# NPM Configuration for Logverse - Private Publishing
registry=https://registry.npmjs.org/

# GitHub Package Registry configuration for @logverse packages (private)
@logverse:registry=https://npm.pkg.github.com

# Authentication token for GitHub Package Registry
# Uncomment and replace YOUR_GITHUB_TOKEN with your actual GitHub Personal Access Token
# //npm.pkg.github.com/:_authToken=YOUR_GITHUB_TOKEN

# Alternative: use npm login command instead of hardcoding token
# Run: npm login --registry=https://npm.pkg.github.com